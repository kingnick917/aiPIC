{
  "name": "project3",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4",
    "next": "^13.1.1",
    "openai": "^3.1.0"
  },
  "engines": {
    "node": ">=14.6.0"
  },

  
  "scripts": {
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "start": "concurrently \"react-scripts start\" \"next start\"",
    "build": "concurrently \"react-scripts build\" \"next build\"" ,
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "dev": "next dev"
  
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
